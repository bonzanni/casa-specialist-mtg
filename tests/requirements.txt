# Test-only dependencies.
#
# The runtime server and the corpus builder are both stdlib-only; these are
# needed by the suite alone. PyYAML parses the workflows so a duplicate key
# cannot reach GitHub — the check that exists because `safe_load` is more
# permissive than GitHub, and which then failed in CI for five consecutive
# pushes because nothing installed it.
pytest
PyYAML

# rfc8785 is optional: the manifest-checksum test computes the canonical
# form itself and only cross-checks against casa's implementation when both
# casa's source and this library are present. Listed so that cross-check
# actually runs for anyone working next to a casa checkout — it was absent,
# so the check silently downgraded to a skip and reported a pass.
rfc8785
